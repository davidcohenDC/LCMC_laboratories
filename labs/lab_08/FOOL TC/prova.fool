let
  var x:int = 5+3;
  fun g:bool (a:bool)
    let
      var s:bool = true;
    in s;
  fun f:bool (n:int, m:int)
    let
      var x:bool = true;
    in x == (n == m);
in
  print (
    g(true)
  );
